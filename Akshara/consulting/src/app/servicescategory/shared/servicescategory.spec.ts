import { addProviders, async, inject } from '@angular/core/testing'
import { Servicescategory } from './servicescategory';

describe('Servicescategory', () => {
  it('should create an instance', () => {
    expect(new Servicescategory()).toBeTruthy();
  });
});
